# Conductor — слепок правок

Дата: 24 августа 2026  
Игра: **Conductor** (бывший «Проводник»)  
README не обновляли — это отдельный слепок кода и геймплея.

---

## 1. Критичные баги (старт сессии)

| Было | Стало |
|------|--------|
| Переход уровня срабатывал каждый кадр, уровень прыгал | Один переход: флаги `isLevelTransitioning` / `isAdvancing` |
| Меню звало `pause()` / `resume()`, методов не было | Есть `pause()`, `resume()`, `stop()` |
| HP блока снимался каждый кадр, пока груз внутри | Урон один раз на вход в блок |
| Рестарт запускал второй игровой цикл | Старый `requestAnimationFrame` отменяется |
| Комбо-бонус капал на каждый удар при комбо 3+ | Бонус только на 3, 6, 9… |
| Цепь из 1 итерации связей, низ экрана зажат | 14 точек, 5 итераций, груз может упасть |
| Сервер принимал любой score | Только конечное неотрицательное число |

---

## 2. Базовый набор из `Улучшние.txt`

1. **Game Over** — затемнение, статистика, «Снова» / «В меню».
2. **Типы блоков** — обычный, крепкий, золотой (50 очков, 5 сек), ледяной.
3. **Визуал** — частицы, встряска, свечение золота, всплывающие `+очки`, сетка и звёзды.
4. **Пауза** — ESC и кнопка ⏸: Продолжить / Заново / В меню.
5. **Звук** — Web Audio API, ползунки общей громкости и эффектов.
6. **Меню** — парящие блоки, пульс кнопок, fade.
7. **Достижения** — 100 очков, 50 блоков за всё время, 3 / 10 / 25 волн за забег.

Три фиксированных уровня из того списка позже заменили бесконечными волнами.

---

## 3. Режим медитации (вместо 3 уровней)

- Волны без потолка: Покой → Дыхание → Поток → Туман → Прилив → Глубина → Тишина → Эхо → Зыбь → Бездна, дальше по кругу.
- С каждой волной больше блоков, больше крепких и льда, короче пауза.
- После волны цепь чуть «отдыхает» (натяжение и износ сбрасываются частично).
- Старт: кнопка «Начать медитацию».

---

## 4. Как рвётся цепь

Раньше ломалась только от резкого рывка мыши. Теперь есть **натяжение** (полоска в HUD и на поле).

| Действие | Эффект |
|----------|--------|
| Плавно вести | Натяжение спадает |
| Резко дёргать мышь | Натяжение растёт |
| Сильно ударить крепкий блок | Скачок натяжения, может сразу порвать |
| Попасть в лёд | Цепь скользит и становится хрупче |
| Много ударов подряд | Износ, рвётся легче |

Экран конца пишет причину: не выдержала / удар / лёд / разрыв / груз упал.

**Лёд ослаблен на ~10%** (последняя правка): меньше скачок натяжения (12 → 10.8), скольжение 1000 → 900 мс, слабее бонус натяжения от рывка и растяжения, быстрее спад.

---

## 5. Баг «мышь ниже цепи»

**Старт:** цепь спавнилась сверху (250, 80), курсор оставался на кнопке. Первый сдвиг считался рывком — мгновенный разрыв.

**Волна:** то же после «Дальше»: курсор на кнопке, цепь на старом месте.

Исправление:

- Цепь ставится **под курсор** при старте, переходе волны и выходе с паузы.
- ~1.4–1.6 с цепь нельзя порвать; в это время она телепортируется за мышью, без растяжения.
- Позиция курсора пишется даже когда игра на паузе / на оверлее.

---

## 6. Название

На экране и во вкладке браузера: **Conductor**.  
Интерфейс по-прежнему на русском.

---

## 7. Файлы

| Файл | Роль |
|------|------|
| `client/index.html` | Разметка, меню, HUD, оверлеи |
| `client/style.css` | Стили, анимации, полоска натяжения |
| `client/physics.js` | Verlet-цепь, скольжение, растяжение |
| `client/render.js` | Canvas: фон, блоки, частицы, цепь, бар натяжения |
| `client/audio.js` | Web Audio |
| `client/achievements.js` | Достижения, localStorage |
| `client/ui.js` | Тосты и оверлеи |
| `client/game.js` | Волны, натяжение, коллизии, переходы |
| `client/menu.js` | Меню и старт |
| `server/index.js` | Статика + рекорд |
| `server/package.json` | `conductor-server` |

---

## 8. Запуск

```bash
cd server
npm install
npm start
```

Открыть http://localhost:3000 и обновить страницу, если сервер уже был запущен.
